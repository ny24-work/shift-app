const DAYS=31,YEAR=2026,MONTH=6,KEY='shiftAppV1';let currentRole='manager',currentName='';const defaultData={pass:{manager:'0000',developer:'9999',staff:''},meals:[65,60,97,63,72,71,65,69,71,107,67,68,74,130,100,87,66,33,47,56,91,100,76,52,53,68,76,56,72,77,78],shifts:{A:{time:'5:00〜14:00',hours:8},B:{time:'5:00〜11:00',hours:6},C:{time:'5:30〜10:30',hours:5},D:{time:'6:30〜11:00',hours:4.5},E:{time:'6:00〜10:30',hours:4.5},F:{time:'タイミー',hours:5}},need:{base:2,60:3,80:4,100:5,120:6},maxConsecutive:5,staffs:[{name:'安藤',maxDays:21,can:['A'],priority:['A'],memo:'A固定'},{name:'永持',maxDays:21,can:['A'],priority:['A'],memo:'A固定'},{name:'宮本',maxDays:21,can:['B','D'],priority:['B','D'],memo:'B/D'},{name:'井阪',maxDays:12,can:['B'],priority:['B'],memo:'B'},{name:'吉田',maxDays:21,can:['C','B','E'],priority:['C','B','E'],memo:'C優先'},{name:'西岡',maxDays:20,can:['D'],priority:['D'],memo:'D固定'},{name:'中西',maxDays:6,can:['A'],priority:['A'],memo:'補助'},{name:'西島',maxDays:4,can:['A'],priority:['A'],memo:'補助'}],requests:{},schedule:{}};let data=load();function load(){try{return JSON.parse(localStorage.getItem(KEY))||structuredClone(defaultData)}catch(e){return structuredClone(defaultData)}}function save(){localStorage.setItem(KEY,JSON.stringify(data))}function $(id){return document.getElementById(id)}function dow(d){return['日','月','火','水','木','金','土'][new Date(YEAR,MONTH,d).getDay()]}function needPeople(m){let n=data.need.base;if(m>=60)n=data.need[60];if(m>=80)n=data.need[80];if(m>=100)n=data.need[100];if(m>=120)n=data.need[120];return n}function isWork(v){return['A','B','C','D','E','F'].includes(v)}function shiftCls(v){return v==='休'?'off':isWork(v)?'shift'+v:''}function init(){$('loginName').innerHTML=data.staffs.map(s=>`<option>${s.name}</option>`).join('');renderAll();if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{})}function login(){currentRole=$('role').value;currentName=$('loginName').value;let p=$('pass').value;if(data.pass[currentRole]&&p!==data.pass[currentRole]){alert('パスコードが違います');return}$('login').classList.add('hidden');$('app').classList.remove('hidden');$('who').textContent=currentRole==='manager'?'店長・管理者':currentRole==='developer'?'やす・開発者':currentName+' さん';document.querySelectorAll('.admin').forEach(el=>el.style.display=currentRole==='staff'?'none':'');if(currentRole==='staff')showTab('request');renderAll()}function demoLogin(){$('role').value='manager';$('pass').value='0000';login()}function showTab(id){document.querySelectorAll('.page').forEach(p=>p.classList.add('hidden'));$(id).classList.remove('hidden');document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));event?.target?.classList.add('active');renderAll()}function renderAll(){renderHome();renderRequests();renderMeals();renderStaffs();renderRules();renderSchedule()}function renderHome(){let c={},h={};data.staffs.forEach(s=>{c[s.name]=0;h[s.name]=0});Object.values(data.schedule||{}).forEach(day=>Object.entries(day).forEach(([n,v])=>{if(isWork(v)&&v!=='F'){c[n]=(c[n]||0)+1;h[n]=(h[n]||0)+(data.shifts[v]?.hours||0)}}));$('homeCards').innerHTML=data.staffs.map(s=>`<div class='card'><b>${s.name}</b><br><span class='muted'>${c[s.name]||0}日 / ${h[s.name]||0}時間</span><br><span class='pill'>${s.memo||''}</span></div>`).join('');$('warnings').innerHTML=getWarnings().join('<br>')||'<span class=good>不足・警告はありません。</span>'}function getWarnings(){let w=[];for(let d=1;d<=DAYS;d++){let need=needPeople(data.meals[d-1]||0),actual=0;Object.values(data.schedule[d]||{}).forEach(v=>{if(isWork(v))actual++});if(Object.keys(data.schedule).length&&actual<need)w.push(`<span class=bad>${d}日：人数不足 ${actual}/${need}</span>`)}return w}function renderRequests(){let names=currentRole==='staff'?[currentName]:data.staffs.map(s=>s.name);$('requestArea').innerHTML=names.map(n=>{let req=data.requests[n]||[],days='';for(let d=1;d<=DAYS;d++)days+=`<label class=reqDay><input type=checkbox data-req="${n}" value="${d}" ${req.includes(d)?'checked':''}>${d}日</label>`;return`<div class=card><h3>${n}</h3>${days}</div>`}).join('')}function saveRequests(){document.querySelectorAll('[data-req]').forEach(cb=>{let n=cb.dataset.req,d=Number(cb.value);data.requests[n]||=[];data.requests[n]=data.requests[n].filter(x=>x!==d);if(cb.checked)data.requests[n].push(d)});save();alert('希望休を保存しました')}function renderMeals(){let h='<tr><th>日</th>';for(let d=1;d<=DAYS;d++)h+=`<th class="${dow(d)==='日'?'sun':dow(d)==='土'?'sat':''}">${d}<br>${dow(d)}</th>`;h+='</tr><tr><th>食数</th>';for(let d=1;d<=DAYS;d++)h+=`<td><input class=smallInput type=number data-meal=${d} value="${data.meals[d-1]||0}"></td>`;h+='</tr><tr><th>必要</th>';for(let d=1;d<=DAYS;d++)h+=`<td>${needPeople(data.meals[d-1]||0)}</td>`;$('mealTable').innerHTML=h+'</tr>'}function saveMeals(){document.querySelectorAll('[data-meal]').forEach(i=>data.meals[Number(i.dataset.meal)-1]=Number(i.value||0));save();renderAll();alert('食数を保存しました')}function renderStaffs(){$('staffArea').innerHTML=data.staffs.map((s,i)=>`<div class=card><div class=grid><div><label>名前</label><input data-staff=${i} data-key=name value="${s.name}"></div><div><label>上限日数</label><input type=number data-staff=${i} data-key=maxDays value="${s.maxDays}"></div><div><label>入れる勤務</label><input data-staff=${i} data-key=can value="${s.can.join(',')}"></div><div><label>優先勤務順</label><input data-staff=${i} data-key=priority value="${s.priority.join(',')}"></div><div><label>メモ</label><input data-staff=${i} data-key=memo value="${s.memo||''}"></div></div></div>`).join('')}function saveStaffs(){document.querySelectorAll('[data-staff]').forEach(i=>{let s=data.staffs[Number(i.dataset.staff)],k=i.dataset.key;if(k==='maxDays')s[k]=Number(i.value);else if(k==='can'||k==='priority')s[k]=i.value.split(',').map(x=>x.trim()).filter(Boolean);else s[k]=i.value.trim()});save();init();alert('保存しました')}function addStaff(){data.staffs.push({name:'新スタッフ',maxDays:10,can:['B'],priority:['B'],memo:''});save();renderStaffs()}function renderRules(){$('shiftArea').innerHTML=Object.entries(data.shifts).map(([k,v])=>`<div class=grid><div><label>${k} 時間</label><input data-shift=${k} data-key=time value="${v.time}"></div><div><label>${k} 勤務時間</label><input type=number step=0.5 data-shift=${k} data-key=hours value="${v.hours}"></div></div>`).join('');$('baseNeed').value=data.need.base;$('need60').value=data.need[60];$('need80').value=data.need[80];$('need100').value=data.need[100];$('need120').value=data.need[120];$('maxConsecutive').value=data.maxConsecutive}function saveRules(){document.querySelectorAll('[data-shift]').forEach(i=>{let s=i.dataset.shift,k=i.dataset.key;data.shifts[s][k]=k==='time'?i.value:Number(i.value)});data.need.base=Number($('baseNeed').value);data.need[60]=Number($('need60').value);data.need[80]=Number($('need80').value);data.need[100]=Number($('need100').value);data.need[120]=Number($('need120').value);data.maxConsecutive=Number($('maxConsecutive').value);save();renderAll();alert('ルール保存しました')}function chooseShift(s){return(s.priority||s.can||['B']).find(x=>(s.can||[]).includes(x))||(s.can||['B'])[0]}function generateSchedule(){let wc={},hh={},con={};data.staffs.forEach(s=>{wc[s.name]=0;hh[s.name]=0;con[s.name]=0});let schedule={};for(let d=1;d<=DAYS;d++){schedule[d]={};data.staffs.forEach(s=>{if((data.requests[s.name]||[]).includes(d)){schedule[d][s.name]='休';con[s.name]=0}else schedule[d][s.name]=''});let assigned=[],need=needPeople(data.meals[d-1]||0);while(assigned.length<need){let cand=data.staffs.filter(s=>!assigned.includes(s.name)&&schedule[d][s.name]!=='休'&&wc[s.name]<s.maxDays&&con[s.name]<data.maxConsecutive&&(s.can||[]).length);if(!cand.length){schedule[d]['タイミー']='F';break}cand.sort((a,b)=>(wc[a.name]-wc[b.name])||(hh[a.name]-hh[b.name])||(con[a.name]-con[b.name]));let s=cand[0],sh=chooseShift(s);schedule[d][s.name]=sh;assigned.push(s.name);wc[s.name]++;hh[s.name]+=data.shifts[sh]?.hours||0;con[s.name]++}data.staffs.forEach(s=>{if(!['A','B','C','D','E'].includes(schedule[d][s.name]))con[s.name]=0})}data.schedule=schedule;save();renderAll();alert('シフト案を作成しました')}function renderSchedule(){let names=[...data.staffs.map(s=>s.name),'タイミー'];let h='<tr><th class=nameCell>名前</th>';for(let d=1;d<=DAYS;d++)h+=`<th class="${dow(d)==='日'?'sun':dow(d)==='土'?'sat':''}">${d}<br>${dow(d)}<br><span class=muted>${data.meals[d-1]||0}</span></th>`;h+='<th>日数</th><th>時間</th></tr>';names.forEach(n=>{let days=0,hrs=0;h+=`<tr><td class=nameCell>${n}</td>`;for(let d=1;d<=DAYS;d++){let v=(data.schedule[d]||{})[n]||'';if(isWork(v)){days++;hrs+=data.shifts[v]?.hours||0}h+=`<td class="${shiftCls(v)}" contenteditable="${currentRole!=='staff'}" onblur="editCell(${d},'${n}',this.textContent)">${v}</td>`}h+=`<td>${days}</td><td>${hrs}</td></tr>`});h+='<tr><th class=nameCell>実績/必要</th>';for(let d=1;d<=DAYS;d++){let actual=0;names.forEach(n=>{if(isWork((data.schedule[d]||{})[n]))actual++});let need=needPeople(data.meals[d-1]||0);h+=`<td class="${actual>=need?'good':'bad'}">${actual}/${need}</td>`}h+='<td></td><td></td></tr>';$('scheduleTable').innerHTML=h}function editCell(day,name,val){val=val.trim().toUpperCase();if(!data.schedule[day])data.schedule[day]={};data.schedule[day][name]=val;save();renderHome()}function exportCSV(){let names=[...data.staffs.map(s=>s.name),'タイミー'],rows=[['名前',...Array.from({length:DAYS},(_,i)=>`${i+1}日`),'日数','時間']];names.forEach(n=>{let row=[n],days=0,hrs=0;for(let d=1;d<=DAYS;d++){let v=(data.schedule[d]||{})[n]||'';row.push(v);if(isWork(v)){days++;hrs+=data.shifts[v]?.hours||0}}row.push(days,hrs);rows.push(row)});downloadText('shift.csv','\ufeff'+rows.map(r=>r.map(x=>`"${String(x).replaceAll('"','""')}"`).join(',')).join('\n'),'text/csv')}function exportBackup(){downloadText('shift-backup.json',JSON.stringify(data,null,2),'application/json')}function importBackup(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{data=JSON.parse(r.result);save();init();alert('読み込みました')};r.readAsText(f)}function downloadText(name,text,type){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click()}function resetAll(){if(confirm('全部初期化する？')){localStorage.removeItem(KEY);data=load();init();alert('初期化しました')}}init();